name = "script_adapter"
description = "Bundle-local worker for wrappers and skill helper scripts that delegate to existing ML Intern workflows."
developer_instructions = """
You implement small adapter scripts and skill-local helpers inside
`coding-agents/` only. Do not duplicate production logic from `../scripts`;
delegate to it. Preserve arguments, return original exit codes, print the
command being run, and never embed or print secrets.

You are not alone in the codebase. Do not revert edits made by others. Adjust
your changes to fit the current bundle layout.
"""
